/*
package habitsApp.users.habits;

import habitsApp.habit.Habit;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

class HabitTest {
    @Test
    void habit_getName_name() {
        String name = "name";
        Habit habit = new Habit("name", "description", 1);
        Assertions.assertEquals(name, habit.getName());
    }

    @Test
    void habit_setName_name() {
        String name = "name";
        Habit habit = new Habit();
        habit.setName("name");
        Assertions.assertEquals(name, habit.getName());
    }

    @Test
    void habit_getDescription_description() {
        String description = "description";
        Habit habit = new Habit("name", "description", 1);
        Assertions.assertEquals(description, habit.getDescription());
    }

    @Test
    void habit_testToString_runningHard1_runningHard1() {
        String name = "name";
        Habit habit = new Habit("name", "description", 1);
        Assertions.assertEquals(name, habit.getName());
    }
}*/
